node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# 1. Replit Specifics (The "Internal" stuff)
.replit
replit.nix
# If you used Replit's built-in DB
replit.db

# 2. Dependencies (Crucial! Never upload these)
node_modules/
dist/
build/
*.tsbuildinfo

# 3. Secrets & Environment
.env
.env.*
# Replit uses a hidden secrets tool, but if you exported to an .env, hide it!

# 4. Logs and Debugging
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 5. OS Specific (Optional but good)
.DS_Store
Thumbs.db

.agent/